import styled from 'styled-components';
const BotComplement = styled.p`
  font-family: 'Nunito Sans', sans-serif;
  font-style: normal;
  font-weight: 400;
  font-size: 12px;
  line-height: 22px;
  text-align: center;
  color: #738192;
  margin: 0;
`;

export default BotComplement;